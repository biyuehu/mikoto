{
  "name": "mikoto",
  "version": "1.0.0",
  "description": "A simple abd tiny image encoding/decoding tool based on Teal. Powered by efficient algorithms, it is designed for evading network content censorship and surveillance.",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "vite",
    "build:prepare": "cyan check src/main.tl && cyan build",
    "build": "vite build"
  },
  "bin": {
    "mikoto": "bin.ts"
  },
  "dependencies": {
    "@jsquash/png": "^3.1.1",
    "lua-in-js": "^2.2.5",
    "wasmoon": "^1.16.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "vite": "^8.1.3"
  }
}